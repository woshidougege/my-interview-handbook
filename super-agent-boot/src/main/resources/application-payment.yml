# 微信支付配置 - 使用V3 API
wx:
  pay:
    appId: wx384b079ac2a54c25
    mchId: 1707461989
    # V3 API 配置
    apiV3Key: 60ff1dff815241a895d17b67751cf62e
    certSerialNo: 76F179E7AE36820B8554D64ADDF6EAA638CED021
    privateCertPath: ${user.home}${file.separator}.goldmine${file.separator}config${file.separator}apiclient_cert.pem
    privateKeyPath: ${user.home}${file.separator}.goldmine${file.separator}config${file.separator}apiclient_key.pem
    publicKeyPath: ${user.home}${file.separator}.goldmine${file.separator}config${file.separator}pub_key.pem
    publicKeyId: PUB_KEY_ID_0117074619892025021900198200000359
    notifyUrl: ${WX_NOTIFY_URL:https://8440-103-90-188-90.ngrok-free.app/api/v1/payment/wechat/notify}
    
  # 支付宝配置已删除 - 系统只支持微信支付
    
  # 订单配置
  order:
    expire-minutes: 15  # 订单过期时间（分钟）
